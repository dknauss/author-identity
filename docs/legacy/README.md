# Legacy Docs

This folder is for documentation kept for historical context rather than current execution.

## What is kept here

- [`../../byline-rss-spec-adoption/`](../../byline-rss-spec-adoption/) remains the primary legacy planning/archive set.

## What was removed

Earlier root-level copies of planning, research, vision, and quality documents were intentionally not preserved here as duplicate files.

Reason:

- the canonical maintained versions now live under `docs/`
- the old root copies added noise without adding a distinct maintenance value
- Git history already preserves those older snapshots if they need to be recovered
