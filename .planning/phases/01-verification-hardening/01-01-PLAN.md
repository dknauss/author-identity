---
phase: 01-verification-hardening
plan: 01-01
type: build
wave: 1
depends_on: []
requirements: ["VER-04"]
files_modified:
  - "byline-feed/tests/phpunit/test-integration-cap.php"
  - "byline-feed/tests/phpunit/test-feed-rss2.php"
  - "byline-feed/tests/phpunit/test-feed-atom.php"
  - "byline-feed/tests/phpunit/test-feed-json.php"
  - "byline-feed/tests/phpunit/test-schema.php"
  - "byline-feed/tests/phpunit/test-rights.php"
  - "byline-feed/tests/e2e/"
  - "docs/quality/TEST_COVERAGE_MATRIX.md"
  - "implementation-strategy/gap-analysis.md"
  - ".planning/phases/01-verification-hardening/01-01-PLAN.md"
autonomous: true
user_setup: []
must_haves:
  truths:
    - "Phase 1 must close the named verification gaps already documented in the repository rather than reopening broad product-scope decisions."
    - "Automated coverage must verify shipped behavior for author ordering, empty-field handling, and special-character handling before the next release cut."
    - "Coverage docs must describe the tests that actually exist after implementation, not aspirational future work."
  artifacts:
    - path: "byline-feed/tests/phpunit/test-integration-cap.php"
      provides: "CAP ordering regression coverage"
    - path: "byline-feed/tests/phpunit/test-feed-rss2.php"
      provides: "RSS2 edge-case hardening coverage"
    - path: "byline-feed/tests/phpunit/test-feed-atom.php"
      provides: "Atom edge-case hardening coverage"
    - path: "byline-feed/tests/phpunit/test-feed-json.php"
      provides: "JSON Feed edge-case hardening coverage"
    - path: "docs/quality/TEST_COVERAGE_MATRIX.md"
      provides: "Updated public record of closed verification gaps"
  key_links: []
---

<objective>
Close the remaining high-value automated coverage gaps called out in the repo quality docs so release readiness is backed by current tests rather than inherited assumptions.
</objective>

<tasks>

<task type="auto">
  <name>01-01-01 Reconcile the documented Phase 1 gaps into an exact implementation checklist</name>
  <files>docs/quality/TEST_COVERAGE_MATRIX.md, implementation-strategy/gap-analysis.md, byline-feed/tests/phpunit/</files>
  <action>
    - Re-read the current coverage matrix and gap analysis and reduce the open work to an exact checklist.
    - Confirm CAP ordering as the one definite open automated gap.
    - Reconcile whether empty-author / empty-field / special-character work is still open or already closed in specific paths, and lock Phase 1 scope to only the still-proven gaps.
  </action>
  <verify>The execution notes map each documented gap to a specific target test file or explicitly defer it out of Phase 1.</verify>
  <automated>`rg -n "author ordering|empty-field|special-character|hardening" docs/quality/TEST_COVERAGE_MATRIX.md implementation-strategy/gap-analysis.md`</automated>
  <done>The remaining verification work is reduced to a concrete implementation checklist.</done>
</task>

<task type="auto">
  <name>01-01-02 Add CAP ordering integration coverage</name>
  <files>byline-feed/tests/phpunit/test-integration-cap.php</files>
  <action>
    - Add regression coverage that exercises the real `get_coauthors()` ordering path rather than only normalization behavior.
    - Assert that the emitted normalized author list preserves author order for multi-author CAP posts.
    - Keep the test shaped around shipped adapter behavior, not CAP internals that are irrelevant to the plugin contract.
  </action>
  <verify>`composer test` covers CAP ordering end-to-end through the adapter path and fails if author order regresses.</verify>
  <automated>`composer test -- --filter test_multi_author_post_returns_all_coauthors_in_order`</automated>
  <done>CAP ordering is covered by committed automated tests.</done>
</task>

<task type="auto">
  <name>01-01-03 Add the remaining proven edge-case tests across shipped output paths</name>
  <files>byline-feed/tests/phpunit/test-feed-rss2.php, byline-feed/tests/phpunit/test-feed-atom.php, byline-feed/tests/phpunit/test-feed-json.php, byline-feed/tests/phpunit/test-schema.php, byline-feed/tests/phpunit/test-rights.php</files>
  <action>
    - Add or strengthen fixtures only where the reconciliation task proves empty-author / empty-field / special-character coverage is still missing.
    - Assert that the affected shipped outputs omit empty optional output correctly and preserve special characters safely.
    - Keep assertions format-specific where needed, but ensure the same normalized-author contract is being exercised across the remaining uncovered paths.
  </action>
  <verify>The affected PHPUnit suites prove omission behavior and special-character handling in every path still identified as open after reconciliation.</verify>
  <automated>`composer test`</automated>
  <done>The remaining proven edge-case hardening gaps are covered by automated tests.</done>
</task>

<task type="auto">
  <name>01-01-04 Refresh representative smoke evidence and update quality docs to match</name>
  <files>byline-feed/tests/e2e/, byline-feed/bin/setup-e2e.sh, docs/quality/TEST_COVERAGE_MATRIX.md, implementation-strategy/gap-analysis.md</files>
  <action>
    - If the gap audit from Task 01-01-01 identifies a release-significant browser or fixture gap, add the smallest deterministic E2E or setup change needed to cover it.
    - Otherwise, explicitly record that the remaining Phase 1 evidence gaps were closed in PHPUnit and no new E2E case was necessary.
    - Update the quality docs so they describe the final evidence accurately and do not continue to list the closed Phase 1 gaps as open.
  </action>
  <verify>`npm run test:e2e` remains green if E2E changed, and the quality docs agree with the final test evidence either way.</verify>
  <automated>`npm run test:e2e && rg -n "CAP adapter — author ordering|special-character|empty-field|hardening" docs/quality/TEST_COVERAGE_MATRIX.md implementation-strategy/gap-analysis.md`</automated>
  <done>Any necessary Phase 1 E2E/fixture hardening is complete and the repository's quality docs match the final evidence.</done>
</task>

</tasks>

<status>
Planned on 2026-04-08.

Execution state:
- Not started.
- This is the first Phase 1 execution slice because later verification runbooks and CI policy should reflect the expanded test evidence produced here.
</status>
