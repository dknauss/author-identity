---
phase: 01-verification-hardening
plan: 01-02
type: build
wave: 2
depends_on: ["01-verification-hardening/01-01"]
requirements: ["VER-01", "VER-02", "VER-03"]
files_modified:
  - "byline-feed/bin/run-phpunit-local.sh"
  - "byline-feed/bin/run-integration-tests.sh"
  - "byline-feed/bin/run-e2e.sh"
  - "byline-feed/package.json"
  - "byline-feed/package-lock.json"
  - ".github/workflows/ci.yml"
  - "docs/quality/RELEASE_CHECKLIST.md"
  - "docs/quality/LOCAL_VERIFICATION.md"
  - ".planning/phases/01-verification-hardening/01-02-PLAN.md"
autonomous: true
user_setup: []
must_haves:
  truths:
    - "Maintainers need one dependable pre-release local verification path rather than several loosely related scripts."
    - "Multisite/integration verification must be runnable end-to-end or documented with exact blockers and prerequisites."
    - "CI trigger policy and local verification guidance must be explicit for behavior-affecting changes before the next release cut."
  artifacts:
    - path: "docs/quality/LOCAL_VERIFICATION.md"
      provides: "Canonical maintainer runbook for local release-significant verification"
    - path: "docs/quality/RELEASE_CHECKLIST.md"
      provides: "Release checklist that points at the maintained verification contract"
    - path: ".github/workflows/ci.yml"
      provides: "Explicit CI coverage / trigger policy aligned with the runbook"
  key_links: []
---

<objective>
Turn the repository's existing local scripts and CI jobs into a clear release-significant verification contract so a maintainer can reproduce the evidence needed for the next cut.
</objective>

<tasks>

<task type="auto">
  <name>01-02-01 Audit the current local verification entrypoints against CI</name>
  <files>byline-feed/bin/run-phpunit-local.sh, byline-feed/bin/run-integration-tests.sh, byline-feed/bin/run-e2e.sh, byline-feed/package.json, .github/workflows/ci.yml</files>
  <action>
    - Inventory the current maintainer-facing commands and what each one actually verifies.
    - Compare those commands with the CI jobs on `main` so the repo has a clear local-to-CI mapping.
    - Record missing prerequisites, duplicated steps, and ambiguous operator choices.
  </action>
  <verify>The audit produces a concrete list of local commands, CI counterparts, prerequisites, and gaps to resolve or document.</verify>
  <automated>`rg -n "run-phpunit-local|run-integration-tests|run-e2e|workflow_dispatch|integration-|phpunit:|playwright|npm run build" byline-feed/bin/run-phpunit-local.sh byline-feed/bin/run-integration-tests.sh byline-feed/bin/run-e2e.sh byline-feed/package.json .github/workflows/ci.yml`</automated>
  <done>The current verification surface is fully mapped before changes are made.</done>
</task>

<task type="auto">
  <name>01-02-02 Create a canonical local verification runbook and command contract</name>
  <files>docs/quality/LOCAL_VERIFICATION.md, byline-feed/package.json, byline-feed/bin/run-phpunit-local.sh, byline-feed/bin/run-integration-tests.sh, byline-feed/bin/run-e2e.sh</files>
  <action>
    - Define one default single-site verification path a maintainer should run before a release cut.
    - Define the exact multisite/integration path and prerequisites, or record the blocker with exact failure conditions if it cannot yet run cleanly.
    - Pin `@wordpress/env` directly or version the `npx` invocation so local browser verification is reproducible.
    - If helpful, add one thin wrapper command so the default path is copy-pasteable and deterministic.
  </action>
  <verify>A maintainer can follow the runbook to execute the single-site path and can either execute or explicitly understand the multisite/integration path.</verify>
  <automated>`composer lint && npm run build`</automated>
  <done>The repo has a canonical local verification contract for release-significant checks.</done>
</task>

<task type="auto">
  <name>01-02-03 Align release checklist and verification docs with the canonical path</name>
  <files>docs/quality/RELEASE_CHECKLIST.md, docs/quality/LOCAL_VERIFICATION.md</files>
  <action>
    - Update the release checklist so it points to the maintained local verification contract instead of leaving operators to infer the right commands.
    - Make the expected evidence outputs clear: what to run, what should pass, and what artifacts or logs matter.
  </action>
  <verify>The release checklist and local verification runbook point to the same commands and expected outcomes.</verify>
  <automated>`rg -n "LOCAL_VERIFICATION|run-phpunit-local|run-integration-tests|test:e2e|verification" docs/quality/RELEASE_CHECKLIST.md docs/quality/LOCAL_VERIFICATION.md`</automated>
  <done>Release-facing verification guidance is consistent and current.</done>
</task>

<task type="auto">
  <name>01-02-04 Make CI trigger policy explicit for behavior-affecting changes</name>
  <files>.github/workflows/ci.yml, docs/quality/LOCAL_VERIFICATION.md</files>
  <action>
    - Review the current workflow path filters and ensure behavior-affecting plugin changes are clearly covered.
    - Add `workflow_dispatch` so maintainers can run a fresh release-verification CI pass on demand.
    - If the existing trigger scope is already correct, document that policy explicitly.
    - If gaps exist, tighten the workflow filters or add explicit notes about when manual re-verification is required.
  </action>
  <verify>The repo leaves behind an explicit answer to: "what changes must cause CI or local release-significant re-verification?"</verify>
  <automated>`rg -n "workflow_dispatch|push:|pull_request:|paths:" .github/workflows/ci.yml`</automated>
  <done>CI trigger policy is explicit and aligned with the maintainer verification contract.</done>
</task>

</tasks>

<status>
Planned on 2026-04-08.

Execution state:
- Not started.
- This is the second Phase 1 slice because it should codify the verification path using the test evidence strengthened in 01-01.
</status>
